function number_cardinality(n) {
    // Planteo:
    if (num.toString() === 0) {
        // zero
    }
    if (num.toString() === 5) {
        // five
    }
    if (n % 2 === 0 && num.toString() !== 0) {
        // even
    }
    if (n % 2 !== 0 && num.toString() !== 5) {
        // odd
    }
}

module.exports = number_cardinality;