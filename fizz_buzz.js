
function fizz_buzz(n) {
    // planteo
    // definir array
    //bucle de 1 a n
    if (n % 3 === 0) {
        // Fizz
    }
    if (n % 5 === 0) {
        // Buzz
    }
    if (n % 3 === 0 && n % 5 === 0) {
        //FizzBuzz
    }
    else {
        // el numero sin cambios
    }
    //push n en el array
    // return array
}

module.exports = fizz_buzz;

